import { PropertyList } from 'crm-felmat';

export function ListaPropiedades() {
  return (
    <div style={{ minWidth: 1000, minHeight: 500, padding: '24px', background: 'var(--background)' }}>
      <PropertyList />
    </div>
  );
}
