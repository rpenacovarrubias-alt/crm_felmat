import { Dashboard } from 'crm-felmat';

export function VistaGeneral() {
  return (
    <div style={{ minWidth: 1000, padding: '0', background: 'var(--background)' }}>
      <Dashboard />
    </div>
  );
}
